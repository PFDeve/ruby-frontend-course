<%= form_with url: '/search', method: :get, data: { action: "ajax:success->main-search#handleResults", target: "main-search.form" } do |form| %>
  <div>
    <%= form.text_field :search, data: { action: "input->main-search#search" }, autocomplete: "off" %>
  </div>
<% end %>